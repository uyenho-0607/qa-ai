# ─── Jira ───────────────────────────────────────────────────────────────────
# Used by: jira-handler scripts (jira_common.py → from jira import JIRA)
jira==3.10.5

# ─── Google Sheets ──────────────────────────────────────────────────────────
# Used by: collect-gsheet-cases (fetch_gsheet_tcs.py → import requests)
#          scripts/mailtm_otp.py (mail.tm REST API for OTP fetching)
#          scripts/mailinator_otp.py (Mailinator public-inbox API for OTP fetching)
# Note: uses raw HTTP to Sheets API — no gspread needed
requests==2.32.3

# ─── Google Sheets API (format_tc_sheet.py) ─────────────────────────────────
# Used by: scripts/format_tc_sheet.py (write, patch, append, insert, remove TCs)
google-api-python-client==2.199.0
google-auth==2.56.3
google-auth-oauthlib==1.4.0

# ─── Utilities ──────────────────────────────────────────────────────────────
# Used by: collect-gsheet-cases (fetch_gsheet_tcs.py → from dotenv import load_dotenv)
python-dotenv==1.0.1
