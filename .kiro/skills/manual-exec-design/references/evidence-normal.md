# ABORTED — superseded by `references/evidence.md`

The `common` + per-mode split failed: `screenshot` mode reached back into the `normal` file for its video
escape hatch. Both modes now live in one file. Load nothing from here.
